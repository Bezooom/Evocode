# -*- coding: utf-8 -*-
"""
Преобразование Markdown заявки в .docx.
#–###### → встроенные заголовки Word 1–9; тело — шрифт 10.5pt; код — Consolas;
для поверенного или для редактирования в Word внутри организации.

Диаграммы: финальная версия использует mermaid_render.py для преобразования mermaid в PNG;
если некоторые блоки не сработали и围栏 сохранены, этот скрипт запишет их как код-блоки.
Скрипт не вызывает mmdc.

Зависимости:
  pip install -r requirements.txt
"""
